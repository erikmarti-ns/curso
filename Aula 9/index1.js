// string, number, undefined, null ou boolean
const nome = 'Erik'; // string
const nome1 = "Erik"; // string
const nome2 = `Erik`; // string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined = não aponta pra local nenhuma na memória
const  sobrenomeAlune = null; // nulo = não apnta pra local nenhuma na memória (eu digo pra a variavel ser nula ser o conjunto vazio)
const aprovado = false; // Boolean = true, false (valor lógico)

console.log(typeof nome, nome) // typeof consigo descobria o tipo de dados como string, number, undefined ou boolean
